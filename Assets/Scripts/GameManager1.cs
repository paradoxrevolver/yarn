using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Update()
    }

    // Update is called once per frame
    void Update()
    {
        //If(InputManager.Restart == true)
        //      make a copy of the GameObject to save
        //      reset the original scene
        //      reset the original GameObject
        //      load the copied GameObject data to the original GameObject
                //(Load score, game progress, etc.)
                //Pins connected = 0
                //Game over state = false
    }
}
