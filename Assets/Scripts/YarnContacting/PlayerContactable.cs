using UnityEngine;

/**
 * This Contactable uniquely belongs to the Player.
 *
 * It's an InteractContactable because interaction is the only way in which the player is added or removed
 * as a Contact to a Yarn object.
 */
public class PlayerContactable : InteractContactable {
    
}